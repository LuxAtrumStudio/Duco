#include "../../operations/operation_headers.hpp"
#include "arithmeticopr.hpp"
#include <iostream>
#include <map>
#include <math.h>
#include <stdarg.h>
#include <string>
#include <vector>

double duco::arithmetic::Add(double val1, double val2) { return (val1 + val2); }

double duco::arithmetic::Sub(double val1, double val2) { return (val1 - val2); }

double duco::arithmetic::Mul(double val1, double val2) { return (val1 * val2); }

double duco::arithmetic::Div(double val1, double val2) { return (val1 / val2); }
